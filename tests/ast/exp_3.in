/*
 * Monga
 *
 * Author: Gabriel de Quadros Ligneul
 *
 * exp_3.in
 */

char[] foo(int x) {
    return new char[x];
}

float bar(int y, int z) {
    return y + z;
}

int boo(char w) {
    return w;
}

int main() {
    int a, b, c, d, e;
    char[] s;
    s = foo(bar(a, boo(b)));
    s = new char[bar(a, b)];
    s[foo(bar(a, b))] = c;
    a = foo(boo(b))[c] + d;
    a = -foo(boo(b))[c + d] * e;
    return 0;
}

