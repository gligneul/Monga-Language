/*
 * Monga
 *
 * Author: Gabriel de Quadros Ligneul
 *
 * exp_1.in
 */

int main() {
    float out, x, y, z;
    out = x + y - z;
    out = x + y + z;
    out = x + y * x;
    out = x / y * x;
    out = -x * y + z;
    out = x + -y * -z;
    out = x -- y -- z;
    out = x * y || z;
    out = x || y * z;
    out = x && y || x && z;
    out = !x || !y;
    out = x - ! - y;
    return 0;
}

