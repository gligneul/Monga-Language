/*
 * Monga
 *
 * Author: Gabriel de Quadros Ligneul
 *
 * accept_func.in
 */

void foo() {
}

int sum(int a, int b) {
    return a + b;
}

char[] search(char[][] strings, int n, char[] string) {
    int i;
    i = 0;
    while (i < n) {
        if (!strcmp(strings[i], string))
            return string;
        i = i + 1;
    }
    return 0;
}

float my_abs(float n) {
    if (num < 0)
        return num;
    return -num;
}
        

