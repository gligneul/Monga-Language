/*
 * PUC-Rio
 * INF1715 Compiladores
 * Gabriel de Quadros Ligneul 1212560
 *
 * accept_exp.in
 */

int empty() {
}

int id(int a) {
    return a;
}

int mul(int a, int b) {
    return a * b;
}

void foo() {
    int x;
    x = 1.0;
    x = x;
    x = x > 2;
    x = 2 < x;
    x = 123 == 321;
    x = 321 >= 123;
    x = 123 <= 321;
    x = 321 != 123;
    x = x + ---x;
    x = -----x------x;
    x = !!!!x&&!!!!x;
    x = x / x;
    x = new int[][][][x||!'a'];
    x = empty() + id(x) + mul(x, x);
    x = null;
    x = true && false;
    x = 3[null][false]; /* var : exp[exp] */
}
